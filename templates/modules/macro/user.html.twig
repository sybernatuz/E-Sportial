{% macro online_status(online) %}
    {% set color = (online) ? 'green' : 'red' %}
    <svg height="100" width="100">
        <circle cx="50" cy="50" r="10" stroke="black" stroke-width="1" fill="{{ color }}"></circle>
    </svg>
{% endmacro %}